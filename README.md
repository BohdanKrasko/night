Create and deploy simple python3 django app

add module to create superuser

add role that install dependancy, pip3 requirements.txt, config nginx and uwsgi

add rc.local that start server after reboot
